{"changes":{"packages/opencode/package.json":"Patch"},"note":"Register /retry-now via an opencode command file written at plugin load, not a config hook. A plugin config hook's config.command mutation is dropped by plugins that rebuild config.command wholesale (e.g. oh-my-openagent), so the command never appeared; command files are scanned by both opencode and such plugins, so /retry-now now registers reliably.","date":"2026-07-02T00:00:00.000Z"}
